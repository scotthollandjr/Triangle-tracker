describe('triTrack', function() {
  it("", function() {
    expect(triTrack()).to.equal();
  });
});


behaviour || input example || output example
a + b <= c || a b c || not a triangle
a = b = c || a b c || equilateral
a = b != c || a b c || isosceles
a != b != c || a b c || scalene
